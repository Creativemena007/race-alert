{
  "name": "race-alert",
  "version": "0.1.0",
  "private": true,
  "description": "Marathon registration notification system",
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "lint": "npm run lint --workspace=apps/web",
    "scrape": "node scripts/scraper.js"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "playwright": "^1.40.0"
  }
}